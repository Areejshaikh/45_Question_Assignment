// 13. Your Own Array: 
var transport = ["Bike", "Bus", "Suzuky", "Civc", "Farari"];
for (var i = 0; i < transport.length; i++) {
    var element = transport[i];
    console.log("I Would Like to own a ".concat(element));
}
